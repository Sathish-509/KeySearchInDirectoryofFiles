{
  "name": "searchfiles",
  "version": "1.0.0",
  "description": "Search for a given string in the given directory with sublevels",
  "main": "Server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Sathish Yerramsetti",
  "license": "ISC",
  "dependencies": {
    "filequeue": "^0.5.0"
  }
}
